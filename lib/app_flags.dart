/// 앱 전체를 켜고 끄는 스위치. **한 줄씩만 둔다.**
///
/// 설정 화면에 노출하지 않는 것들이다. 켜려면 값을 바꾸고 다시 빌드한다.
library;

/// 지도 위 **도심 확대 인셋**을 보일지.
///
/// **기본은 꺼짐이다**(2026-08-19 M10 사용자 판단). 실제로 써 보니 작은 구를
/// 찾을 때 인셋보다 **검색(M3)을 더 쓴다**는 것이 드러났다. 기능 자체는
/// 남겨 두되 화면에서는 뺀다 — 지도를 가리는 값을 하지 못했다.
///
/// 코드와 테스트는 그대로 살아 있으므로 이 값만 `true` 로 바꾸면 돌아온다.
/// 긁기 단위를 광역시 단위로 줄이는 결정이 나면 **인셋은 필요 자체가 사라진다**
/// — 도심 작은 구가 없어지기 때문이다. 그때 지울지 정한다.
const bool kShowMapInset = false;
