# -*- coding: utf-8 -*-
"""긁기 단위 병합 명세 — 에셋 생성 쪽 원본. **단일 원본은 아니다** (아래 참고).

시군구를 그대로 쓰지 않고 하나로 합치는 긁기 단위를 정의한다.
지도 에셋 생성(`make_asset.py`)이 이 파일을 읽는다.

## 실제로는 단일 원본이 아니다 (2026-08-14 확인)

이 docstring 은 원래 "카테고리 배정도 같은 이 파일을 읽는다" 고 적고 있었으나
**사실이 아니다.** Codex 검토(2026-08-14)로 드러났고 직접 확인했다.
현재 병합 대상은 **세 곳에 중복**돼 있다.

1. 이 파일의 `MERGE`
2. `design/README.md` 의 mapshaper 명령 — 시도 이름을 직접 하드코딩
3. `source/android/tool/category/make_category_map.py` — `11000`·`50000` 을 직접 하드코딩.
   이 파일을 import 하지 않는다

통합하지 않은 이유는 **저장소 경계** 때문이다. 이 파일은 `design/` 에 있고 그곳은
git 저장소가 아니다. 카테고리 생성기가 이 파일을 import 하면 앱 저장소만 clone 했을 때
생성·검증이 불가능해진다 — 2026-08-13 Codex 검토에서 정확히 그 이유로 생성기를
앱 저장소 안으로 옮겼다.

통합하려면 이 파일을 앱 저장소로 옮기는 결정이 선행돼야 한다. **미결 항목이다.**
그때까지는 세 곳을 함께 고쳐야 한다.

2026-08-14 사용자 결정: 서울 25개 구와 제주 2개 시를 각각 하나로 합친다.
긁기 단위 256개 → **231개**.

## 왜 빌드 타임인가

런타임(앱 로딩 시) 병합을 먼저 설계했다가 Codex 검토에서 폐기했다.

- 카테고리 생성기가 256개 에셋을 직접 읽고 코드 집합 일치를 실패 조건으로 걸어 두어
  **반드시 깨진다**
- `Region.rings` 를 이어 붙이면 아트 배치의 `largestRingBounds` 가 통합 지역이 아니라
  **가장 큰 구**를 고른다 (서울=서초구, 제주=서귀포시). 남산타워가 서초구에 놓인다
- 앱이 보는 231개와 생성기가 보는 256개로 **단일 원본이 둘로 갈린다**

빌드 타임이면 셋 다 사라진다. 되돌리기도 이 파일의 항목을 지우고 재생성하면 된다.

## 코드 규칙

시도 코드 2자리 + `000`. 서울 구는 11110~, 제주는 50110~ 이라 충돌하지 않는다.
**통계청 `sgg` 5자리가 아니다** — `PROJECT_BRIEF.md` 의 지역 코드 원칙에 대한 명시적 예외다.
그래서 앱과 서버는 이 값을 `sgg` 가 아니라 **`scratchUnitId`** 로 부른다
(앱 반영 완료 — `Region.scratchUnitId`, 2026-08-14).
"""

# 시도 이름 -> 통합 긁기 단위
MERGE = {
    '서울특별시': {
        'code': '11000',
        # 이름을 시도명과 같게 둔다. UI 는 시도명과 지역명이 같으면 한 번만 보여준다.
        'name': '서울특별시',
    },
    '제주특별자치도': {
        'code': '50000',
        'name': '제주특별자치도',
    },
}

#: 병합 대상 시도 이름 집합.
MERGED_SIDOS = set(MERGE)

#: 통합 코드 집합.
MERGED_CODES = {v['code'] for v in MERGE.values()}


def unit_for(sidonm, sgg, sggnm):
    """이 시군구가 속할 긁기 단위의 (코드, 이름)."""
    m = MERGE.get(sidonm)
    return (m['code'], m['name']) if m else (sgg, sggnm)


def is_merged(code):
    return code in MERGED_CODES
